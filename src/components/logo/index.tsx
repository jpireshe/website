import React from "react";
import Icon from "../../images/logo.svg";

const Logo = (): React.ReactElement => {
  return <img src={Icon} alt="Logo" className="COMPONENTS-logo" />;
};

export default Logo;
